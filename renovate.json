{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["local>eleboucher/renovate-config"],
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 5am on monday"]
  },
  "packageRules": [
    {
      "description": "Helm 4 mandates plugin provenance verification (breaks the helm-unittest git source) and changed `helm push` output the chart-signing step parses",
      "matchDepNames": ["helm"],
      "allowedVersions": "<4"
    },
    {
      "description": "clienthello 0.2 pins aes-gcm =0.11.0-rc.3, which cannot coexist with our stable aes-gcm 0.11; drop once upstream relaxes the pin",
      "matchPackageNames": ["clienthello"],
      "allowedVersions": "<0.2"
    },
    {
      "description": "Keep rust-toolchain.toml and mise.toml in sync on Rust bumps",
      "matchDepNames": ["rust"],
      "groupName": "rust toolchain"
    },
    {
      "description": "Group all minor and patch updates into a single PR",
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "all non-major dependencies",
      "groupSlug": "all-non-major"
    },
    {
      "description": "No breaking '!' on major updates; a dep/CI bump must not force a product major",
      "matchUpdateTypes": ["major"],
      "commitMessagePrefix": "{{semanticCommitType}}({{semanticCommitScope}}):"
    }
  ]
}
