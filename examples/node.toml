[identity]
key_path = "node.key"

[hub]
enabled = false

[edge]
enabled = true
listen_addr = "127.0.0.1:8443"
# Hub(s) this edge subscribes to for route updates. The edge rotates
# through the list on disconnect for hub failover.
hub_urls = ["https://hub.example.eu:8443"]
