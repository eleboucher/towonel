[identity]
key_path = "agent.key"

# Hex-encoded iroh EndpointIds of trusted edges.
# If empty, all connections are accepted (insecure, for testing only).
trusted_edges = []

[[services]]
hostname = "test.example.eu"
origin = "127.0.0.1:9090"
