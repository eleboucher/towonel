---
apiVersion: v2
name: towonel-agent
description: A Helm chart for the towonel agent - origin-side connector for towonel tunnels
type: application
# Floor matching the chart's surface: restricted Pod Security (seccompProfile)
# and the optional monitoring CRDs (ServiceMonitor, PrometheusRule, GrafanaDashboard).
kubeVersion: ">=1.25.0-0"
version: 0.0.0
appVersion: "0.0.0"
home: https://codeberg.org/towonel/towonel
sources:
  - https://codeberg.org/towonel/towonel
maintainers:
  - name: towonel
keywords:
  - towonel
  - tunnel
  - agent
annotations:
  category: Networking
