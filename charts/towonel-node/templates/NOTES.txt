{{ .Chart.Name }} {{ .Chart.AppVersion }} deployed as {{ .Values.controllers.main.type }} {{ include "bjw-s.common.lib.chart.names.fullname" . }}.

Role is set via the TOWONEL_HUB_ENABLED / TOWONEL_EDGE_ENABLED env and the
configured listeners/ports. The chart defaults to a hub Deployment; apply the
edge overlay (values-edge.yaml) for a hostNetwork DaemonSet edge.

Values reference:
https://codeberg.org/towonel/towonel/src/branch/main/charts/towonel-node/README.md
