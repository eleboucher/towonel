suite: node serviceaccount
templates:
  - common.yaml.tpl
tests:
  - it: creates a ServiceAccount by default
    documentSelector:
      path: kind
      value: ServiceAccount
    asserts:
      - isKind:
          of: ServiceAccount
